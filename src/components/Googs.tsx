import { GoodsProps } from "../types/goods";


const Googs = (props: GoodsProps) => {
     
    const {id, title, price, category, description, image} = props

    return (
        <div>
            
        </div>
    );
};

export default Googs;